source 'https://rubygems.org'
gem 'jekyll'
gem 'jekyll-sitemap'
gem 'jekyll-redirect-from'
gem 'hebruby'
gem 'filesize'

gem 'rmagick'

gem 'wdm', '>= 0.1.0' if Gem.win_platform?

group :jekyll_plugins do
  gem 'generate_thumbnails_plugin', '0.1', :path => File.join('.', '_vendor', 'generate_thumbnails_plugin')#, :require => 'thumbnails'
end
